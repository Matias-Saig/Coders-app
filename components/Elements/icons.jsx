import { AntDesign } from "@expo/vector-icons";

const Icons = ({ refer, size, color }) => {
  return <AntDesign name={refer} size={size} color={color} />;
  // <AntDesign name="home" size={24} color="black" />
};

export default Icons;
